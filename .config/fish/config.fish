if status is-interactive
	macchina -o host -o machine -o kernel -o distribution -o desktop-environment -o window-manager -o packages -o terminal -o shell -o uptime -o processor -o resolution -o backlight -o processor-load -o memory
    # Commands to run in interactive sessions can go here
end
